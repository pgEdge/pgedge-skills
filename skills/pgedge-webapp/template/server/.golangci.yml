version: "2"
run:
  timeout: 5m
linters:
  default: none
  enable:
    - bodyclose
    - errcheck
    - gosec
    - govet
    - misspell
    - revive
    - staticcheck
    - unconvert
    - unused
formatters:
  enable:
    - gofmt
    - goimports
