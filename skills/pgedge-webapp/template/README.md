# <PROJECT_NAME>

Scaffolded by the `pgedge-webapp` skill. Run `pgedge-docs` to generate
proper user-facing documentation.

## Quick start

Run `make dev` (or `./start_dev.sh`) and open http://localhost:5173.
The seed admin account is `admin` with the password set at scaffolding
time.
